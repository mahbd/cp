#include <bits/stdc++.h>

using namespace std;
#define ll long long
#define fast_input                    \
    ios_base::sync_with_stdio(false); \
    cin.tie(0)
#define tc()          \
    ll test_case;     \
    cin >> test_case; \
    for (ll tcv = 0; tcv < test_case; tcv++)

ll calculate_cost(const string &s, )

int main() {
    fast_input;
    tc() {
        ll a, b;
        string s;
        cin >> a >> b >> s;

    }
    return 0;
}
